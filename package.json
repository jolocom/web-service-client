{
  "name": "@jolocom/web-service-client",
  "version": "1.0.1",
  "description": "Browser client for services based on @jolocom/web-service-base",
  "main": "dist/index.js",
  "repository": "https://github.com/jolocom/web-service-client",
  "author": "Jolocom Dev <dev@jolocom.io>",
  "license": "Apache-2.0",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "typescript": "3.6.3"
  },
  "peerDependencies": {
  },
  "dependencies": {
  },
  "scripts": {
    "prepare": "yarn build",
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc -p src"
  }
}
